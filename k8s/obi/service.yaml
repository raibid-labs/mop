apiVersion: v1
kind: Service
metadata:
  name: obi-metrics
  namespace: observability-dev
  labels:
    app: obi
    component: metrics
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: obi
  ports:
  - name: metrics
    port: 8888
    targetPort: 8888
    protocol: TCP